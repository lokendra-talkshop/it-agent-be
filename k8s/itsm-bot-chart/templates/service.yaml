apiVersion: v1
kind: Service
metadata:
  name: {{ include "itsm-bot-chart.fullname" . }}
  labels:
    {{- include "itsm-bot-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "itsm-bot-chart.selectorLabels" . | nindent 4 }}
